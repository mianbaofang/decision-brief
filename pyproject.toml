[project]
name = "choice-skill"
version = "0.9.0"
description = "Local-first AI decision assistant with Web, CLI, and Codex Skill interfaces"
license = { text = "MIT" }
requires-python = ">=3.9"
authors = [{ name = "Choice Skill Contributors" }]
readme = "README.md"
keywords = ["decision-making", "decision-support", "ai-assistant", "local-first", "fastapi", "sqlite", "openai-compatible", "codex-skill"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Framework :: FastAPI",
]
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic>=2",
    "python-multipart",
    "edge-tts",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "httpx", "playwright"]

[project.scripts]
choice-assistant = "scripts.choice_assistant:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
addopts = "-v"

[tool.ruff]
line-length = 120
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["backend", "backend.models", "backend.routes", "backend.services", "scripts"]
